#include<stdio.h>
#include<stdlib.h>
struct tree
{
    int data;
    struct tree *lft;
    struct tree *rht;
};
int main()
{
    int Emp, Exis_Debt, Coll, sav, ten, amt, cr_scr, age;
    TREE:
    struct tree *head = NULL;
    struct tree *n1 = (struct tree*)malloc(sizeof(struct tree));
    n1->lft = 0;
    n1->rht = (struct tree*)malloc(sizeof(struct tree));
    return 0;
}