#include<bits/stdc++.h>
using namespace std;
string encrypted(string str, string key) {
    vector<int> v(key.size(), 0);
    for(int i = 0; i < key.size(); i++) {
        
    }
}
int main() {
    string str, key;
    cout<<"Input: ";
    getline(cin, str);
    cout<<"Key: ";
    getline(cin, key);

    return 0;
}